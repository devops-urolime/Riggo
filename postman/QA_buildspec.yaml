version: 0.2

phases:
  install:
    runtime-versions:
        nodejs: 8
  pre_build:
    commands:
      - npm install newman --global
      - npm install newman-reporter-html --global
  build:
    commands:
      - cd postman
      - newman run -r html,cli RiggoAPI.postman_collection.json -e QA.postman_environment.json  --reporter-html-export report-`date '+%y%m%d-%H%M%S'.html

artifacts:
  files:
    - report-`date '+%y%m%d-%H%M%S'.html
  base-directory: 'postman'
